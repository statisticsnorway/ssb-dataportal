apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ssb-dataportal
  title: SSB Dataportal
  description: |
    Gateway to SSB's shared knowledge about data
  tags:
    - metadata
  annotations:
    github.com/project-slug: statisticsnorway/ssb-dataportal
spec:
  type: service
  owner: dapla-metadata-developers
  lifecycle: production
  dependsOn:
    - system:dapla-lab
    - component:dapla-toolbelt-metadata
    - component:ssb-dash-components
  consumesApis:
    - api:vardef
  system: metadata
