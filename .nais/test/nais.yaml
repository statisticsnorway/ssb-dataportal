apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  labels:
    team: dapla-metadata
  name: dataportalen
  namespace: dapla-metadata
  annotations:
    # Allow nextjs to create cache directories inside /app
    nais.io/read-only-file-system: "false"
spec:
  ingresses:
    - https://dataportal.test.ssb.no
  port: 3000
  accessPolicy:
    outbound:
      rules:
        - application: metadata-api-gateway
        - application: klass
      external:
        - host: auth.test.ssb.no
  login:
    provider: openid
    enforce:
      enabled: true
  replicas:
    max: 1
    min: 1
  envFrom:
    - secret: dataportalen
  env:
    - name: VARDEF_BASE_PATH
      value: http://metadata-api-gateway
    - name: VARDEF_USE_STATIC_DATA
      value: "false"
    - name: VARDEF_USE_M2M_TOKEN
      value: "true"
    - name: KEYCLOAK_HOST
      value: https://auth.test.ssb.no
    - name: KLASS_BASE_PATH
      value: http://klass/api/klass/v1
    - name: KLASS_USE_STATIC_DATA
      value: "false"
    - name: DAPLA_LAB_VARDEF_URL
      value: "https://lab.dapla-test.ssb.no/launcher/dapla-lab-standard/vardef-forvaltning"
    - name: LOG_LEVEL
      value: "info"
    - name: NEXT_PUBLIC_LOGIN_URL
      value: https://dataportal.test.ssb.no/oauth2/login
    - name: NEXT_PUBLIC_LOGOUT_URL
      value: https://dataportal.test.ssb.no/oauth2/logout
